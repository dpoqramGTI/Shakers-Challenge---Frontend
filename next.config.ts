// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "logo.clearbit.com",
      // otros dominios de logos que aparezcan en mock
    ],
  },
};

module.exports = nextConfig;
